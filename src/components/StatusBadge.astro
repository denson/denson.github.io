---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-gray-900 border border-gray-800 text-sm text-gray-300">
  <span class="w-2 h-2 rounded-full bg-green-500"></span>
  <span>{text}</span>
</div>
